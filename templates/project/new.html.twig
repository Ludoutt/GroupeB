{% extends 'base.html.twig' %}

{% block title %} Ajouter un projet {% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mb-5 mt-5">Nouveau projet</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
            <button type="submit" class="btn btn-success mt-3">Valider</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
